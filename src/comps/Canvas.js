import React from "react";
import styled from "styled-components";

const Canvas = (props) => {
  return <CanvasContainer id="c"></CanvasContainer>;
};

export default Canvas;

const CanvasContainer = styled.canvas`
  background-color: black;
`;
